{
  "name": "@mua/nn",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "scripts": {
    "test": "vitest",
    "dev": "tsup src/index.ts --watch --dts --clean --format cjs,esm",
    "build": "tsup src/index.ts --dts --clean --format cjs,esm"
  },
  "dependencies": {
    "@mua/tensor": "workspace: *"
  }
}
